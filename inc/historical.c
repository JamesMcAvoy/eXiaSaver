#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <time.h>
#include <unistd.h>

void writeHist(Historical hist) {
/*
	FILE* fichier = NULL;
	fichier = fopen("historique.txt", "a+");
	
	if (fichier != NULL) {
		system("date +\"%F %T\" > histo.txt");
		fclose(fichier);
	}
*/
}